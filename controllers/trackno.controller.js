export const TrackIndex = (req,res) => {
    res.send("Get all mobile numbers");
  };

export const Acccreate =(req,res) => {
    res.send("Create an account");
 };

export const Accupdate=(req,res) =>{
    res.send("Update the details");
 };

export const Accdelete=(req,res) => {
    res.send("Delete the phone number");
};